{
  "version": 2,
  "framework": null,
  "buildCommand": "pnpm --filter client run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
